//----------------------------------------------------------------------------------------
// File: programs.view.jsx
//
// Desc: ...
// Path: /src/components/routes/programs/programs.view
//----------------------------------------------------------------------------------------


import React, { memo } from 'react';
// import PropTypes from 'prop-types';
import { hot } from 'react-hot-loader/root'; // Gestore dell'hot-reloading della route

const Programs = (props) => (
  <div className="bg-programs" />
);


Programs.propTypes = {
};

Programs.defaultProps = {
};


export default hot(memo(Programs));
